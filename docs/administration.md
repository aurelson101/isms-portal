# Administration

L’accès `/admin` exige l’un des groupes `ISMS_ADMIN_GROUPS`. Les administrateurs
gèrent groupes synchronisés, règles, espaces, documents, LDAP, CA publiques,
audit, santé et configuration. Toute mutation durable doit produire un événement
d’audit sans secret.

