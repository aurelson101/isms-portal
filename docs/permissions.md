# Permissions

Les règles associent groupe AD et espace avec `showMenu`, `read`, `search`,
`preview`, `download`, `upload`, `edit`, `publish`, `archive` et `administer`.
L’absence de règle vaut refus. L’API filtre listes et recherche avant de
retourner des métadonnées et contrôle aussi les accès directs.

